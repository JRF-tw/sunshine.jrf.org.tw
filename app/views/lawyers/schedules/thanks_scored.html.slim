= render layout: 'components/score_card',
  locals: { status: "thanks_scored", type: 'schedule' }

  = render partial: 'components/schedule_thanks_scored', locals: {\
    form_url: check_date_lawyer_score_schedules_path,
    path_to_root: lawyer_root_path,
    current_user: current_lawyer,
    toggle_subscribe_story_path: \
    toggle_lawyer_subscribe_path(@record.story, format: :js),
  }
