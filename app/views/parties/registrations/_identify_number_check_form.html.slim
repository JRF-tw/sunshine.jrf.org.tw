= simple_form_for(:party, as: resource_name, url:  party_registrations_check_identify_number_path, method: :post ) do |f|
  .form-group
    = f.input :name, required: true, autofocus: true, label: false
    = f.label :name
  
  .form-group
    = f.input :identify_number, required: true, label: false
    = f.label :identify_number
  
  .form-group
    = check_box_tag :policy_agreement
    = label_tag :policy_agreement, "我已詳細閱讀服務條款和隱私權保護政策"

  .form-actions
    .form-group.text-center = f.button :button, "下一步", class: "raised-button--large"

    .text-center = link_to "登入", new_session_path(resource_name), class: "flat-button--large"