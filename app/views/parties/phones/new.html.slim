= render layout: 'components/card', locals: { heading: '輸入手機號碼' }
  = simple_form_for @phone_form,
    url: party_phone_path,
    method: :post,
    wrapper: :md do |f|
    
    = f.input :unconfirmed_phone, label: '手機號碼'

    = render 'components/form_actions',
      f: f,
      submit: '寄送驗證碼'
