.container
  .card
    h2.card__heading 觀察員註冊

    = simple_form_for(resource, as: resource_name, url: court_observer_registration_path) do |f|
      = f.error_notification

      .form-group
        = f.input_field :name, required: true, autofocus: true
        = f.label :name

      .form-group
        = f.input_field :email, required: true
        = f.label :email

      .form-group
        = f.input_field :password, required: true
        = f.label :password

      .form-group
        = f.input_field :password_confirmation, required: true
        = f.label :password_confirmation

      .form-group
        = check_box_tag :policy_agreement
        = label_tag :policy_agreement, "我已詳細閱讀服務條款和隱私權保護政策"
      
      .form-actions
        .form-group.text-center = f.button :button, "註冊", class: "raised-button--large"

        .text-center = link_to "登入", new_session_path(resource_name), class: "flat-button--large"