= render layout: 'components/card',
  locals: {\
    heading: heading,
    wide: true,
    type: type,
    name: name,
    username: username\
  }

  .user-info-wrapper
    = yield

  = render 'components/user_info_actions'