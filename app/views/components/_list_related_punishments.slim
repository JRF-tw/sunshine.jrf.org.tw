ul.list--arrow
  - @newest_punishments.each do |punishment|
    li
      - if punishment.summary.present?
        = link_to summary_text(punishment.summary, 10), profile_punishment_path(@profile, punishment)
      - elsif punishment.reason.present?
        = link_to summary_text(punishment.reason, 10), profile_punishment_path(@profile, punishment)
      - else
        = link_to "決定機關：#{render_punishment_decision_unit(punishment)}", profile_punishment_path(@profile, punishment)