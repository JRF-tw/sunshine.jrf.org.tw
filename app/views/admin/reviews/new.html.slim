= admin_widget_box "新增#{@owner.name}的相關新聞評論", icon: :pencil do
  = simple_form_for @review, url: send("admin_#{@owner_singularize}_reviews_path", @owner), method: :post, wrapper: :admin do |f|
    = render partial: "form", object: f, as: :f
