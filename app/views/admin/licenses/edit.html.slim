= admin_widget_box "編輯#{@profile.name}的專業證書", icon: :pencil do
  = simple_form_for @license, url: admin_profile_license_path(@profile, @license), method: :put, wrapper: :admin do |f|
    = render partial: "form", object: f, as: :f
