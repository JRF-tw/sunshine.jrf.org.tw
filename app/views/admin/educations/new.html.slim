= admin_widget_box "新增#{@owner.name}的學經歷", icon: :pencil do
  = simple_form_for @education, url: send("admin_#{@owner_singularize}_educations_path", @owner), method: :post, wrapper: :admin do |f|
    = render partial: "form", object: f, as: :f
