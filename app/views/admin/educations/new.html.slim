= admin_widget_box "新增#{@profile.name}的學經歷", icon: :pencil do
  = simple_form_for @education, url: admin_profile_educations_path(@profile), method: :post, wrapper: :admin do |f|
    = render partial: "form", object: f, as: :f
