= admin_widget_box "新增#{@owner.name}的學經歷", icon: :pencil do
  = simple_form_for @education, url: polymorphic_path([@owner, :educations]), method: :post, wrapper: :admin do |f|
    = render partial: "form", object: f, as: :f
