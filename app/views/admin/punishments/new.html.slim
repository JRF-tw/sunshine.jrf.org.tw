= admin_widget_box "新增#{@owner.name}的懲處紀錄", icon: :pencil do
  = simple_form_for @punishment, url: polymorphic_path([@owner, :punishments]), method: :post, wrapper: :admin do |f|
    = render partial: "form", object: f, as: :f
