= admin_widget_box "新增#{@owner.name}的懲處紀錄", icon: :pencil do
  = simple_form_for @punishment, url: send("admin_#{@owner_singularize}_punishments_path", @owner), method: :post, wrapper: :admin do |f|
    = render partial: "form", object: f, as: :f
