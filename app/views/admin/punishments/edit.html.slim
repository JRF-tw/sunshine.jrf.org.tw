= admin_widget_box "編輯#{@owner.name}的懲處紀錄", icon: :pencil do
  = simple_form_for @punishment, url: polymorphic_path([@owner, @punishment]), method: :put, wrapper: :admin do |f|
    = render partial: "form", object: f, as: :f
