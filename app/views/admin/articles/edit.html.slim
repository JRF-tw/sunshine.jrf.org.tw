= admin_widget_box "編輯#{@owner.name}的發表言論列表", icon: :pencil do
  = simple_form_for @article, url: send("admin_#{@owner_singularize}_article_path", @owner, @article), method: :put, wrapper: :admin do |f|
    = render partial: "form", object: f, as: :f
