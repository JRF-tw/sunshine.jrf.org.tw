= admin_widget_box "新增#{@suit.title}的案例處理經過", icon: :pencil do
  = simple_form_for @procedure, url: admin_suit_procedures_path(@suit), method: :post, wrapper: :admin do |f|
    = render partial: "form", object: f, as: :f
