= f.input :career_type
= f.input :old_unit
= f.input :old_title
= f.input :old_assign_court
= f.input :old_assign_judicial
= f.input :old_pt
= f.input :new_unit
= f.input :new_title
= f.input :new_assign_court
= f.input :new_assign_judicial
= f.input :new_pt
= f.input :start_at, as: "string", hint: "半型純數字，民國，格式：YY/MM/DD，如不清楚確切日期就填該年第一天或該月第一天"
= f.input :end_at, as: "string", hint: "半型純數字，民國，格式：YY/MM/DD，如不清楚確切日期就填該年最後一天及該月最後一天"
= f.input :publish_at, as: "string", hint: "半型純數字，民國，格式：YY/MM/DD"
= f.input :source
= f.input :source_link
= f.input :origin_desc
= f.input :memo
.form-actions
  = f.submit "送出", class: "btn btn-primary"
  = " "
  = link_to admin_profile_careers_path(@profile), :class => "btn" do
    = "回到#{@profile.name}的職務經歷列表"