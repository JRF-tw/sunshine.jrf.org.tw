= admin_widget_box "新增#{@profile.name}的獎勵記錄", icon: :pencil do
  = simple_form_for @award, url: admin_profile_awards_path(@profile), method: :post, wrapper: :admin do |f|
    = render partial: "form", object: f, as: :f
