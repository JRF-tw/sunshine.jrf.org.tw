= admin_widget_box "新增#{@owner.name}的獎勵紀錄", icon: :pencil do
  = simple_form_for @award, url: polymorphic_path([@owner, :awards]), method: :post, wrapper: :admin do |f|
    = render partial: "form", object: f, as: :f
