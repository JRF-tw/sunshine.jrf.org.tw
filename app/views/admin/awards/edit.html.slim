= admin_widget_box "編輯#{@owner.name}的獎勵紀錄", icon: :pencil do
  = simple_form_for @award, url: send("admin_#{@owner_singularize}_award_path", @owner, @award), method: :put, wrapper: :admin do |f|
    = render partial: "form", object: f, as: :f
